/**
 * Named constants for the reframe core. Aspect ratios are expressed as `width / height`.
 */

/** Portrait short-form target (TikTok/Reels/Shorts): `9 / 16`. */
export const ASPECT_9_16 = 9 / 16

/** Landscape source aspect (standard 16:9): `16 / 9`. */
export const ASPECT_16_9 = 16 / 9

/** Default sampling rate (frames per second) for `cropRectsOverTime` when none is supplied. */
export const DEFAULT_TIMELINE_FPS = 30
