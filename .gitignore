# dependencies
/node_modules

# next.js
/.next/
/out/

# typescript
*.tsbuildinfo
next-env.d.ts

# drizzle / sqlite database (runtime data, never committed)
/data/
*.db
*.db-journal
*.sqlite
*.sqlite3

# render output + per-run logs (runtime artifacts)
/output/
/logs/

# whisper.cpp ggml models (large binaries, downloaded per-machine — never committed)
/models/

# env
.env
.env*.local

# editor / os
.DS_Store
*.log
